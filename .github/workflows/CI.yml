name: CI

on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: read

jobs:
  test:
    uses: ./.github/workflows/test.yml
  lint:
    uses: ./.github/workflows/pre_commit.yml
  package:
    uses: ./.github/workflows/package.yml
